// export default function Button(props) {
//   return <button class="btn-green right">{props.children}</button>;
// }

export default function Button({ children }) {
  return <button class="btn-green right">{children}</button>;
}
